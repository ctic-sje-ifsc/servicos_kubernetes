kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: netbox-postgresql-base
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 2Gi
  storageClassName: netbox-postgresql
